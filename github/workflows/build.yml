# on:
#   push:
#     branches:
#       - main
name: build (ci) workflow

on: 
  pull_request:
    branches:
      - main
jobs:
  test:
  runs-on: ubuntu-latest
  container:
    image: node:20
    


